

<div class="travelImage">
<div class="white">
<h2  id="where"> Where are you traveling to?</h2>
<button id="asia" type="button">Middle East & Asia</button>
<button id="australia" type="button">Australia</button>
<button id="europe" type="button">Europe</button>
<button id="americas" type="button">Americas</button>



	<div class="travelto1">
	<h1>Middle East & Asia</h1>
		
		<% @city.each do |city| %>
		<% if city.asia == true %>
		<p id="cityName"><%= link_to "#{city.name}", city_path(city.id), {:style=> 'font-size:22px; color:black;'} %></p><br/>
		<% end %>
		<% end %>
		
	</div>

	<div class="travelto2">
	<h1>Australia</h1>
		<ul>
		<% @city.each do |city| %>
		<% if city.australia == true %>
		<p id="cityName"><%= link_to "#{city.name}", city_path(city.id), {:style=> 'font-size:22px; color:black;'} %></p><br/>
		<% end %>
		<% end %>
		</ul>
	</div>

	<div class="travelto3">
	<h1>Europe</h1>
		<ul>
		<% @city.each do |city| %>
		<% if city.europe == true %>
		<p id="cityName"><%= link_to "#{city.name}", city_path(city.id), {:style=> 'font-size:22px; color:black;'}%></p><br/>
		<% end %>
		<% end %>
		</ul>
	</div>

	<div class="travelto4">
	<h1>Americas</h1>
		<ul>
		<% @city.each do |city| %>
		<% if city.america == true %>
		<p id="cityName"><%= link_to "#{city.name}", city_path(city.id), {:style=> 'font-size:22px; color:black;'} %></p><br/>
		<% end %>
		<% end %>
		</ul>
	</div>
	</div>
</div>




