<h1>Cities#new</h1>
<p>Find me in app/views/cities/new.html.erb</p>

	    <%= form_for @city, :url => cities_path, :html => { :multipart => true } do |f| %>
	  
	     <div class="field1">
		<%= f.label :City %>
		<%= f.text_field :name,  class: 'form-control'  %> <br />
		</div>
		<div class="continue"> <%= f.submit :Continue %></div>
