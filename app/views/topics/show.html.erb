
<div id="topicPhoto">
	<div id="topicBody2">

<h4 class id="commentPage"><%="Topic Started By: #{@topic.user.fname}"%></h4>
<h4 class id="commentPage"><%="#{@topic.title}"%></h4>
<ul>
<%@topic.comments.each do |comment|%>

	<p><div id="comment3"><%= image_tag comment.user.avatar.url(:thumb),  {:style=> 'border-radius:20px;'} %><%=" - #{comment.body}"%></br></br></div><div id="commentBody"><%=" Posted: #{comment.created_at.strftime('%a %b %d, %l:%M:%S')} "%>|<%=" #{comment.user.fname} - Traveling: #{comment.user.departure.strftime(" %b. %d, %Y")} - #{comment.user.arrival.strftime(" %b. %d, %Y")}"%></div></p>
	<%end%>
</ul>
<div id="backOne">
<%= link_to "Respond to Post", new_city_destination_topic_comment_path(@city, @dest, @topic) %></br>
</br>
<%= link_to "Back to #{@dest.name}", city_destination_topics_path(@city, @dest) %>
</br>
</br>
<%= link_to "Back to #{@city.name}", city_path(@city.id) %>
</div>
</div>
</div>